PORT=8080
NODE_ENV=production
DB_PATH=./database.sqlite
BACKUP_DIR=./backups

# Redis Configuration
REDIS_URL=redis://localhost:6379
REDIS_PREFIX=viscord:
USE_REDIS=true

# Legacy Mode (set to true to disable Redis and use old broadcast system)
USE_LEGACY_BROADCAST=false
